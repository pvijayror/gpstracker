.form-actions
  = link_to raw(%|<i class="icon-arrow-left icon-white"></i>&nbsp;#{t("actions.back")}|), back, :class => "btn btn-info"
  = render "shared/actions/common", :edit => edit, :destroy => destroy, :instance => instance, :associations => associations